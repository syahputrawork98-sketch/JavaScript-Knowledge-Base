# C03 Examples

Contoh untuk chapter `C03-try-catch-dasar.md`.

## File

- `example.js`: menangkap error dari `JSON.parse()` yang gagal.
- `example-02.js`: membaca `error.name` dan `error.message` di dalam `catch`.
- `example-03.js`: memberi fallback sederhana saat parsing gagal.

## Cara Menjalankan

```bash
node example.js
node example-02.js
node example-03.js
```
