# B03 - Expressions, Operators, dan Statements

Fondasi membentuk logika program dan kontrol alur berbasis sumber ECMAScript.

## Status

- Seluruh bab `C01-C32` sudah terisi penuh.
- Folder `examples/` lengkap per bab (`example.js`, `example-02.js`, `example-03.js`, `README.md`).
- Folder `assets/` sudah berisi 32 SVG visual map (`C01-C32`).
- Chapter `C01-C32` sudah terhubung ke `Visual Map` masing-masing.
- `CHANGELOG.md` aktif mencatat perubahan materi, examples, dan aset.

## Struktur

- `chapters/` untuk materi utama.
- `examples/` untuk contoh runnable per bab.
- `assets/` untuk visual pendukung.
- `docs/` untuk aturan khusus buku.
