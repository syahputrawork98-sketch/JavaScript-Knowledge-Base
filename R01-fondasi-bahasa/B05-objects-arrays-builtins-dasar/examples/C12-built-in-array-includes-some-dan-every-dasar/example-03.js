const scores = [60, 75, 90];
const mixedScores = [60, 45, 90];

console.log('scores every >= 60 ->', scores.every((score) => score >= 60));
console.log('mixedScores every >= 60 ->', mixedScores.every((score) => score >= 60));
