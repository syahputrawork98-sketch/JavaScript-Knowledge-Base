# B05 - Objects, Arrays, dan Built-ins Dasar

Fondasi struktur data inti JavaScript untuk bekerja nyaman dengan object, array, dan built-in yang paling sering dipakai.

## Status

- Blueprint bab `C01-C16` sudah ditetapkan.
- Materi chapter `C01-C16` sudah tersedia.
- Folder `examples/` sudah tersedia lengkap untuk chapter `C01-C16`.
- Batch visual map `C01`, `C05`, `C08`, `C09`, `C10`, `C12`, `C14`, dan `C15` sudah tersedia di folder `assets/`.

## Cakupan Utama

- `C01-C04`: fondasi object, property access, mutation, dan pemeriksaan property.
- `C05-C08`: fondasi array, iterasi dasar, dan operasi mutable yang paling sering dipakai.
- `C09-C12`: transformasi data dasar dan built-ins inti `Object`/`Array`.
- `C13-C16`: built-ins praktis, `JSON`, reference behavior, dan debugging data dasar.

## Struktur

- `chapters/` untuk materi utama.
- `examples/` untuk contoh runnable per bab.
- `assets/` untuk visual pendukung.
- `docs/` untuk aturan khusus buku.
