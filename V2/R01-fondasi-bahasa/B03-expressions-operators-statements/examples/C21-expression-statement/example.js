// C21 - Expression Statement

let total = 0;
total += 5; // expression statement (assignment)

function logResult(value) {
  console.log('hasil:', value);
}

logResult(total); // expression statement (function call)

'teks ini dievaluasi tapi hasilnya tidak dipakai';
