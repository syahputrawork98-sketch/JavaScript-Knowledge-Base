// C02 - Primary Expressions

const literalNumber = 42;
const literalString = 'hello';
const literalArray = [1, 2, 3];
const literalObject = { id: 1 };

const grouped = (1 + 2) * 3;

console.log('literalNumber:', literalNumber);
console.log('literalString:', literalString);
console.log('literalArray length:', literalArray.length);
console.log('literalObject.id:', literalObject.id);
console.log('grouped:', grouped);
