// C04 - Update Expressions

let x = 1;
const prefix = ++x;

let y = 1;
const postfix = y++;

console.log('prefix result:', prefix, '| x:', x);
console.log('postfix result:', postfix, '| y:', y);

let counter = 0;
counter++;
counter--;
console.log('counter akhir:', counter);
